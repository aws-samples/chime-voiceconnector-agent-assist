"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const folder_hash_1 = require("folder-hash");
async function hashDir(dir, exclude) {
    const { hash: folderHash } = await folder_hash_1.hashElement(dir, {
        folders: { exclude: exclude },
    });
    return Buffer.from(folderHash)
        .toString('hex')
        .substr(0, 20);
}
module.exports = {
    hashDir,
};
//# sourceMappingURL=hash-dir.js.map